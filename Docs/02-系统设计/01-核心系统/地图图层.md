> 状态：草稿
> 程序实现：无

# 地图图层

| 字段 | 内容 |
|------|------|
| 状态 | 草稿 |
| 校验状态 | 未校验 |
| 日期 | 2026-06-21 |
| 相关设定 | 无 |
| 相关系统 | [地图与移动](./地图与移动.md)、[荒野地点](../02-资源循环/荒野地点.md)、[队伍系统](./队伍系统.md)、[回合与行动表](../02-玩法循环/回合与行动表.md) |

## 目标

定义正六边形格子上多层内容的组成方式、各图层的堆叠规则，以及单位与环境在他人行动阶段的响应机制。

## 范围

- **包含**：图层类型、默认可叠加规则、各图层堆叠逻辑、图层间影响规则与优先级、响应触发条件与示例。
- **不包含**：影响规则的具体数值、完整规则表、程序实现。

## 详细说明

### 基本原则

- 一个格子可同时含有多个图层的内容。
- 一般情况下，不同图层的内容可在同一格内叠加。
- 在此基础之上，存在图层之间的**影响逻辑**；各图层内部也有各自的重叠逻辑。

### 图层间影响逻辑

图层之间默认可堆叠；当多种图层内容同时存在时，按**影响规则**修正互动结果。

**优先级（必须）**

- 每一条影响规则**必须**绑定独立的优先级。
- 结算时按优先级顺序依次判定；优先级数值越小，越先结算。
- 即使两条规则对调顺序不会改变最终结果（例如密林与丘陵对哨塔视野的叠加），每条规则仍须各自持有优先级，不得省略。

### 影响规则示例

| 优先级 | 触发条件 | 效果 |
|--------|----------|------|
| 10 | 地形层为**裂隙** | 该格**不可**在设施层建立任何设施 |
| 20 | 地形层为**裂隙**，且存在任意一对**对向格**（裂隙两侧相对的两格），两格的地形均**非裂隙** | 允许在该裂隙上建立**桥梁**类设施 |
| 30 | 设施层存在**哨塔**，地形层为**密林** | 削弱哨塔视野 |
| 40 | 设施层存在**哨塔**，地形层为**丘陵** | 强化哨塔视野（程度待定） |
| 50 | 设施层存在**哨塔**，地形层为**山脉** | 强化哨塔视野（程度高于丘陵，数值待定） |

说明：

- 优先级 10 与 20 同时判定时：先排除一般设施，再评估是否满足桥梁例外。
- 优先级 30、40、50 同时作用于同一哨塔时，按序依次结算；具体叠加方式（累加 / 取极值等）待定，但各条规则的优先级不可合并省略。

### 图层一览

由下至上，典型叠放顺序如下：

| 图层 | 内容 | 堆叠规则 |
|------|------|----------|
| 地形层 | 当地默认地形：平原、丘陵、沼泽、密林、山脉、裂隙等 | 每格一种，作为底层 |
| 环境层 | 非天生的环境效果：迷雾、沙暴、移动城市的轨迹等 | 各环境类型自有重叠逻辑 |
| 资源层 | 资源点；允许建造对应种类的开采点 | 不堆叠同类，但允许多种资源并存；统计资源储量 |
| 建筑层 | 城市、村镇、遗迹等 | 不可堆叠 |
| 设施层 | 功能设施：地雷、哨塔、驿站、开采基地等（与建筑层区分） | 堆叠规则受已有设施影响 |
| 物品层 | 可被单位拾取的物品 | 堆叠上限受其他图层影响 |
| 单位层 | 队伍单位 | 默认不可堆叠；部分设施/建筑可容纳多个单位 |

### 图层与地点类型的关系

- **建筑层**中的城市、村镇，与 [荒野地点](../02-资源循环/荒野地点.md) 中的外部据点对应；玩家**移动城市**占格逻辑见 [城市模块化](../03-模块与城市/城市模块化.md)。
- **设施层**中的驿站、开采基地等，与 [探索与扩张](./探索与扩张.md) 中的建设行为对应。
- **资源层**与 [四种核心资源](../02-资源循环/四种核心资源.md) 的产出来源相关。

### 响应

环境或单位可具备**响应**机制：

- 在**其他单位**的[行动阶段](../02-玩法循环/回合与行动表.md#回合阶段)内，若有符合条件的单位执行了符合条件的行动，则触发该环境或单位的响应行为。
- 响应在目标单位的行动环节内即时结算，可打断其当前行动。

**示例（陷阱）**

- 陷阱属于设施层的一种设施。
- 某单位在自己的行动环节内，从陷阱**相邻格**移动进入陷阱所在格时，陷阱响应：
  1. 打断目标单位行动；
  2. 对目标造成伤害；
  3. 消耗该陷阱的耐久。

## 待确认事项

- [ ] 影响规则完整清单与各条优先级编号方案。
- [ ] 哨塔视野在密林、丘陵、山脉同时存在时的叠加结算方式。
- [ ] 「对向格」的精确定义与桥梁设施的占地范围。
- [ ] 移动城市轨迹在环境层的留存时长与叠加规则。
- [ ] 响应的优先级与多响应同时触发时的结算顺序。

## 修订记录

| 日期 | 版本 | 说明 |
|------|------|------|
| 2026-06-21 | 0.1.0 | 初稿 |
| 2026-06-21 | 0.2.0 | 补充图层间影响逻辑、优先级要求与示例规则；正文首次提及行动阶段时加链 |
