**校验状态**：待校验  
**最后更新**：2026-07-19

# 待细化追踪 · 程序

记录**实现、SO/CSV、服务、测试、运行时数据结构、API 守卫、Unity 代码桩**等程序设计侧尚未落地或待对齐的问题。

← [00-规范](./README.md) · [系统轨](./待细化追踪-系统.md) · [设定轨](./待细化追踪-设定.md) · [策划表轨](./待细化追踪-策划表.md)

## 如何维护

| 事项 | 约定 |
|------|------|
| **何时新增** | [03-程序设计](../03-程序设计/) 文档或代码侧发现阻塞实现的缺口；或系统设计已定、待 SO/服务/测试矩阵落地时 |
| **ID 命名** | 本轨 **`sf-NN`**（两位序号，从 01 递增）；与 [设计缺口清单](../03-程序设计/设计缺口清单.md) 主题对齐时互链 |
| **状态取值** | **已定**（规格已写入程序文档）· **部分符合**（占位/SO 草案已有、实现未完成）· **待讨论**（技术方案未定） |
| **闭环** | 实现或规格写入 [03-程序设计](../03-程序设计/) → 移入「已关闭项」→ 同步关闭 [设计缺口清单](../03-程序设计/设计缺口清单.md) 对应行 |
| **交叉项** | 玩法规则未定 → 主归属 [系统追踪](./待细化追踪-系统.md)；本轨只记**程序落地**子项 |
| **策划已定** | 招募/效忠等 gate 规则正文在 [势力系统](../02-系统设计/05-城市与领袖/势力系统.md) 与 [city-capability-flags](../03-程序设计/03-数据字典/city-capability-flags.md)；本轨只跟落地 |
| **禁止臆测补全** | 无权威依据时**不得**当场写实现或 spec；须登记 open 项后停手 → [文档库使用说明 · §10](./文档库使用说明.md#10-开放问题处理agent-与维护者) |

### 与本轨其它清单的关系

| 文档 | 记什么 | 不记什么 |
|------|--------|----------|
| **本文件（程序轨）** | **代码 / 服务 / SO / 测试** 尚未实现或仅原型 | 玩法规则争议（→ 系统轨 sy） |
| [设计缺口清单](../03-程序设计/设计缺口清单.md) | 系统设计**是否已定**、阻塞程序建表的**规则缺口**；与 sy 互链 | 不宜重复登记已在本轨逐条列出的实现项 |
| 各程序文档「待确认事项」 | 单表 / 单模块细节 | — |
| [粮食与周总结 · 程序落地](../01-草稿/归档/粮食与周总结/粮食与周总结-程序落地.md) 等待建表 | 子模块级**待建类清单** | 闭环后条目升入本轨或关闭 |

**现状**：游戏内目前以**原型 / MVP** 为主（格点选中、格周指挥 UI、回合行动表骨架、GAS-lite 薄切片、部分设施建造运维）；[模块划分](../03-程序设计/01-架构总览/模块划分.md) 所列 **Core / Turn / Map / City / Resource / Team / Sun / Exploration / 交战 / 周总结** 等**多数模块尚未完整落地**——本轨 2026-07-11 起按模块补全开放项。

## 当前开放项

### 架构与呈现

| ID | 问题 | 优先级 | 状态 | 备注 |
|----|------|--------|------|------|
| sf-01 | 3D 渲染管线选型（URP / HDRP 等） | P2 | 待讨论 | [平台与操作 · 范围](../02-系统设计/01-核心体验/平台与操作.md#范围)、[引擎映射](../03-程序设计/04-引擎映射/README.md) |
| sf-34 | 地图显示模块后续：L2～L4 图层上屏、视野迷雾、表驱动配色、正六边形外形 | P2 | 待讨论 | 初版**已落地**（`Yanxu.MapView`：显示模型 + DTLM 色卡颜色占位显示、平顶朝向已裁定）→ [地图显示模块](../03-程序设计/02-运行时逻辑/地图显示模块.md)；后续与 sf-01（渲染管线）、sf-13（图层导入管线）联动 |
| sf-36 | 游戏日志运行时：`GameLogService`、消息等级与待办判定 | P2 | 待讨论 | 规则框架 sy-38（三种消息等级 info/warn/error、已知/待办双模式）；需新建**日志条目**运行时对象（等级、消息正文、待办标识、关联实体引用）、**日志列表存储**、**待办消息判定与自动消解**服务（按回合末或事件驱动触发重判）、**日志→地图指针联动**清除服务；提醒到游戏日志的通路规则（哪些事件触发哪条消息）**待定** → [平台与操作 · 游戏日志](../02-系统设计/01-核心体验/平台与操作.md#游戏日志) |

### 城市、势力与 gate（sf-02 簇）

| ID | 问题 | 优先级 | 状态 | 备注 |
|----|------|--------|------|------|
| sf-02 | 城市统一运行时与 NPC 能力开关 | P1 | 部分符合 | 规则已定 → [city-capability-flags](../03-程序设计/03-数据字典/city-capability-flags.md)；**求值服务已落地**（`CityCapabilityService` + `CityCapabilityFlagsSO`）；**测试矩阵已落地**（`CityCapabilityServiceTests` T-01~T-29 全通过，2026-07-17；T-11 暂以 `IsMobileCityDocked=true` 绕开 sf-04 判定顺序偏差） |

| 子 ID | 说明 | 状态 |
|-------|------|------|
| sf-03 | `CityCapabilityFlagsSO` / 各 gate 与策划表映射 | 部分符合 | **SO 已创建**，含 `CreateDefault()` 默认规则矩阵；接入 `GameSessionBootstrap.CityCapability` |
| sf-04 | `CityCapabilityService`：未效忠 / 效忠 / 占领 / 废墟 / 航行等组合判定顺序 | 待讨论 | **服务已落地**（构造 + 求值入口）；判定顺序逻辑仍需策划裁定 |
| sf-05 | 城市管理 UI 控件与 gate 同一数据源（sy-23） | 部分符合 |
| sf-06 | `occupation_trigger`、贸易、编组等跨模块集成测试（T-01～T-29） | 待讨论 |
| sf-07 | `recruitment-office-data`、`extract_reserve`、征兵办运行时 | 部分符合 |

### 回合、工作与城市拓扑

| ID | 问题 | 优先级 | 状态 | 备注 |
|----|------|--------|------|------|
| sf-08 | 回合阶段完整推进与环境行动挂钩 | P1 | 部分符合 | 势力行动表 / 指令表**原型已有**；`EncounterInterruptHandler` **已落地**（暂停/恢复）；`ActionTableExtensions` 静态扩展类**已就绪**（插入/插队/阵亡清理）；[回合与行动表](../02-系统设计/07-玩法循环/回合与行动表.md) 全阶段、外部城排序 **待落地** |
| sf-09 | 工作系统运行时：`interrupt_behavior` -1/0/n、精密工作中断、多 `work_type` | P1 | 部分符合 | 停泊/航行切换、装卸货等**部分**；`AdvanceWithParticipants` 增量公式**已落地**；`WorkZoneOverheadService` **已落地**；**拆解/修复城区工作类型已注册**（2026-07-17：`dismantle_district` / `repair_district` 工作类型 + `TeamCommandKind` 映射补全，完成分发在 `PrototypeContext` 原有 `OnWorkCompletedDismantleDistrict` / `OnWorkCompletedRepairDistrict` 接线）；**修复改为每回合独立结算**（2026-07-19：不再积满后一次性触发，每回合按工作量比例结算恢复量与金属消耗；达 100% 自动终止、金属不足该轮终止）；**城区链接为即时操作、不经过工作管线**（sy-15 已闭合，见 [sf-10](#sf-10) / [sf-39](#sf-39)）；sy-19 闭合带来新待办（见子项）→ `WorkTypeConfigSO` 字段同步走 [sf-38](#sf-38) |

| 子 ID | 说明 | 状态 |
|-------|------|------|
| sf-09a | `interrupt_behavior` 字段类型从字符串枚举改为 int（-1=不可暂停 / 0=可暂停 / n>0=需维持衰减 n）；`WorkTypeConfigSO` + 运行时推进逻辑同步 | 待落地 |
| sf-09b | `is_precision_work` 精密工作中断接线：被攻击时（依赖 sf-24 战斗管线 `OnAttacked` 事件）或城市航行移动时（依赖 sf-11 `OnCityMoveStep` 事件），触发精密工作中断；中断后进度处置由 `interrupt_behavior` 决定 | 待落地 |
| sf-10 | 城市拓扑：**城区链接**修建/拆除、编辑连接、迁移链接重组、`mobile_city_id` 连通分量 | P1 | 部分符合 | 规则已定 → [连接与多核心 · 链接与连接拓扑](../02-系统设计/03-图层与地点/建筑层/连接与多核心.md#链接与连接拓扑)；**运行时图结构已实现**（`DistrictConnectionGraph`：BFS 连通分量 + 分离判定 + `removedEdges` 原址重建追踪）；**`LinkConfigSO` 已落地**（修建 15 / 回收 1 / 重建 12 金属）；**① 链接即时命令入口已落地**（2026-07-19：`LinkCommandService`——`TryBuildLink`（邻接校验+城市金属扣除+原址重建折扣）/ `TryDismantleLink`（连通性保护+金属回收），经 `GameSessionBootstrap.LinkCommands` 接线，编译零错误）；**待落地**：② 编辑连接模式（点选相邻格切换、确认时一次结算新建/保留/拆除多条）；③ 迁移城区链接重组（规划时预约资源、迁移完成结算时一并执行）→ [分离与拆解](../02-系统设计/03-图层与地点/建筑层/分离与拆解.md) |
| sf-37 | 初始城市结构配置化（开局布局） | P1 | 部分符合 | **2026-07-17 落地**：`InitialCityLayoutConfigSO`（城区清单：类型编号 / 占格 / 完整度 / 正常与废墟状态 / 承载与居民 / 工作区启闭 + 链接拓扑 + 初始人口与仓库）、`InitialCityAssembler`（校验 + 装配 + 人口播种，校验失败回退旧星形注册）、默认资产 `Configs/InitialCityLayout_Main`、`GameSessionBootstrap.RegisterCityFromLayout` 与 `PrototypeContext` 接线；**校验已含**：链接两端占格相邻（对齐 [连接与多核心 · 拓扑示例](../02-系统设计/03-图层与地点/建筑层/连接与多核心.md#链接与连接拓扑) 已定规则）、废墟状态与结构完整度一致（低于阈值即废墟，阈值取 `DistrictDismantleRepairConfigSO.ruinThreshold`）、城区占格落在地图内；**待定**：城区类别 SO（当前 `districtTypeId` 仅字符串引用 [L4_district_defs.csv](../03-程序设计/03-数据字典/tables/L4_district_defs.csv)）、开局已分离城区暂不支持、开局数值（人口 120 / 居民 60 / 粮 200 / 金属 100）为原型占位待策划裁定 |
| sf-11 | 停泊 / 航行：`MobileCityState`、边界守卫与单位进出 | P1 | 部分符合 | sy-19 全部闭合；MVP 入口守卫已有但**需重做**（见子项）；`MobileCityState` 状态机已有 |
| | | | | |

| 子 ID | 说明 | 状态 |
|-------|------|------|
| sf-11a | 移除旧 `UnitBoarding` / `UnitDisembark` 入口守卫（旧版航行态禁止登录/脱离，现改为开放） | 待落地 |
| sf-11b | 移除旧版城市移动时不可通过的阻挡逻辑（现单位可自由经过航行城市 footprint） | 待落地 |
| sf-11c | 搭载随城移动：登录在城市上的单位随城市 footprint 整体移动，移动过程中不独立执行指令 | 待落地 |
| sf-11d | 倾轧 + AI 规避：城市航行移动对行进路径上单位造成倾轧→立即阵亡（无论友方敌方）；城市与棋子 AI 主动避免此状况 | 待落地 |
| sf-12 | 工作区：`WorkZoneState`、日常开销 / 激活开销结算 | P1 | 部分符合 | 规则已定 → [运作与居民](../02-系统设计/03-图层与地点/建筑层/运作与居民.md)；`WorkZoneOverheadService` **已落地**（`AdvanceTurn` 日常扣减 + `TryPayActivationCost` 激活扣减）；`DistrictModuleCostConfigSO` 默认参数已填（core_district/academy/dockyard/cannon）；watchtower 条目**已删除**（侦察塔已移为野外设施）。设施行与模块行 **待统一 UI 数据源** |

### 地图、太阳与图层

| ID | 问题 | 优先级 | 状态 | 备注 |
|----|------|--------|------|------|
| sf-13 | 六图层 CSV → 运行时：`L1`～`L5` 加载与格状态合成 | P1 | 部分符合 | 表结构见 [地图图层配置数据结构](../03-程序设计/03-数据字典/地图图层配置数据结构.md)；**导入与校验管线已落地**（2026-07-17：`DesignTableCsv` 通用解析器 + `DesignTableImporter` Editor 菜单——「校验策划表（L0~L6）」主键唯一/列数/必填校验、「导入 L1 地形表」生成 `Resources/Configs/TerrainDefs.asset`；`TerrainDefsSO` 运行时查询 + `FacilityPathCostProvider.OverrideCosts` 注入路径消耗）；**SO 类型已落地**（`EnvironmentDefsSO` / `DistrictDefsSO` / `FacilityDefsSO` / `CrossModifiersSO`）；**L0/L2/L4/L5 导入菜单已落地**（2026-07-19：`DesignTableImporter` 新增「导入 L0 跨层修正表」「导入 L2 环境定义表」「导入 L4 城区定义表」「导入 L5 设施定义表」菜单项，分别生成 `Resources/Configs/CrossModifiers.asset` / `EnvironmentDefs.asset` / `DistrictDefs.asset` / `FacilityDefs.asset`；asmdef 引用已补 `Yanxu.City` / `Yanxu.Environment` / `Yanxu.Facility`；编译零错误）；**待落地**：格状态合成运行时 |
| sf-15 | 太阳照射区：`v(t)=v₀+a·t`、格修正、`sun_motion_enabled` | P1 | 部分符合 | 边界规则 sy-01 部分已定；**Sun 模块已落地**（`SunMotionService` 每 7 回合推进 n=n₀+a·t、步进向下取整、光带三分类查询；`EnvironmentOrchestrator.RunEnvironmentPhase` Step 1 已接，2026-07-17 核对）；**`LightBandService` 已落地**（统一光带查询 + 便捷判断 + 停用事件转发）；**`SunMotionService.OnSunMotionDisabled` 事件已落地**（照射区移出地图时触发）；**待接**：光带对格修正 / 移动停用的下游效果（暗渊 debuff 接入、环境编排器跳过检测确认等，依赖 sy-01 具体效果裁定）

### 资源、粮食与城市管理

| ID | 问题 | 优先级 | 状态 | 备注 |
|----|------|--------|------|------|
| sf-16 | 四类资源库存与指令消耗分轨 | P1 | 部分符合 | 金属 / 能源 / 人口**原型占位**；完整产出消耗链 **待建**（`04-数值框架/` 延后） |
| sf-17 | **粮食周总结**全链路 | P1 | 部分符合 | ⚠ 原待建类清单文件 [已移除](../01-草稿/归档/粮食与周总结/)；规则见 [粮食与周总结数据结构](../03-程序设计/03-数据字典/粮食与周总结数据结构.md)；**代码已存在**：`WeeklySummaryOrchestrator`、`FoodAllocator`、`FoodSufficiencyIndicator`、`WeeklySummaryFoodSteps`、`TeamCargoState`、`EscortAccompanyingState`；完整链路集成是否完成**待确认** |
| sf-18 | 城市管理系统：`cargo_node`、三 Tab 运行时与持久化 | P1 | 部分符合 | 框架 sy-23；`FoodSufficiencyIndicator`、存储策略 **待建** |

### 队伍、视野、设施与探索

| ID | 问题 | 优先级 | 状态 | 备注 |
|----|------|--------|------|------|
| sf-19 | 编组 / 补员：预占用、相邻格三池、`form_team` / `reinforce_team` | P1 | 部分符合 | 规则 sy-03 已定；`TeamFormationService` **已落地**（预占用 + 取消）；`ReinforcementOrchestrator` **已落地**（人口扣减 + 队伍人数更新 + 人数系数曲线）；`HeadcountFactorCurve` **已落地**（线性/自定义 AnimationCurve 驱动）；完整编组→补员 UI 流程 **待接** |

### 阻塞项：实现侧等待规则裁定

以下程序任务被系统设计侧尚未闭合的规则缺口阻塞，在对应 sy 闭环前**不启动开发**。

| 子 ID | 说明 | 阻塞规则 | 涉及 sf |
|-------|------|----------|--------|
| sf-block-01 | 委托模板池正式工厂：各委托类型的子类名单、奖励在 +10～+20 区间的取值规则、3 条槽位中各类型的刷出权重 | **sy-28**（待裁定） | sf-31 |
| sf-block-02 | 外部城市是否与玩家同一粮食周总结 | **sy-33**（待讨论） | sf-26 |
| sf-block-03 | 外部城市减员 / 库存 / 产兵节拍 | **sy-33**（待讨论） | sf-26 |
| sf-block-04 | 玩家是否可见外部城市 AI 经济摘要 | **sy-33**（待讨论） | sf-26 |
| sf-20 | 队伍能力 executor：侦察 / 建造 / 运维 / 运输 / 攻击 | P1 | 部分符合 | GAS-lite + 薄切片 GA；**已落地**：`TryBuild` / `TryMaintain` executor；`TryProspect` 随勘探撤退废弃待清理；**侦察进入视野即时揭示已接线**（`TeamVisionService` 扫描能力 → `RevealIntegrationBridge`，2026-07-16）；**运输链已补全**（2026-07-17：`TeamCommandKind.LoadCargo` / `UnloadCargo` 指令 + `LoadCargoExecutor`（`TryLoadCargo`）+ `unload_cargo` 工作类型 + 完成分发在 `PrototypeContext` 接线）；`TeamCargoLoadService` 超载丢弃逻辑已有，**装卸的具体物品转移量待策划裁定**（当前完成时仅记日志） |
| sf-21 | 资源点揭示即时写入玩家已知层 | P1 | 部分符合 | 规则 sy-06 已定；`ResourceRevealService` **已落地**；`RevealIntegrationBridge` **已落地**（侦察进入视野→种类与储量一并揭示）；**地图情报层已接**（`VisionKnownLayerStore` 已知层 + `TeamVisionService` 覆盖重算，2026-07-16）；**迷雾查询与渲染层已落地**（`FogStateProvider`：Unexplored/Known/Visible 三态查询 + `FogRenderer`：六边形遮罩 mesh 实例化 + 三态材质切换 + MarkDirty 增量刷新）；**待落地**：迷雾材质资产（三个实际材质的 ShaderGraph 或 Shader 文件）、Bootstrap 接线（`FogRenderer.Bind(fogProvider, gridStore)`）
| sf-22 | 设施：L5 表驱动建造条件、运维、城墙、通道等扩展 | P1 | 部分符合 | **2026-07-17 上轮落地**：耐久/运维量对齐 CSV、增量运维、通道启用、多地形标签、航道/仓库统一。**2026-07-17 本轮落地**：温室 ResourceConvert（枚举新增 + ProductionService 扣输入→产输出，能源 50→食物 150）；建造消耗真实扣减（LifecycleService 接入城市库存，金属/资源建造前校验扣除）；桥梁几何守卫（方向 0-5 持久化至 site→instance 全链 + 对向两端非阻断地形校验 + `HexCoord.GetNeighborAt` 辅助）；**航道效果接线**（`SailingGridDamageService` 航行每格结构损伤 + 航道减半；`FacilityPathCostProvider` 地形消耗表 + 通道/航道步长→1，经 `HexPathService.FindPath` 新增 `getCellCost` 回调接入单位寻路与移动结算，城市移动同步平移连接图占格坐标）；**失效与残骸**（`FacilityDestructionService`：耐久≤20% 转残骸、`IsRuin` 占位、回收 10% 金属、同类型残骸格重建 80% 资源；地形类失效后果经 `OnFacilityDestroyed` 事件在 `PrototypeContext` 接线——格内单位阵亡 + 城区航行脱离并额外受 40% 结构损伤；`recycle_facility` 工作类型 + 完成分发已注册）；**回收残骸指令入口已建**（2026-07-17：`TeamCommandKind.RecycleFacility` + `RecycleFacilityExecutor`（`TryRecycle`，校验目标为残骸）+ 工作类型注册）；**交战损伤已接 ApplyDamage**（`DefaultCombatStructureResolver.ApplyStructureDamage` → `FacilityDestructionService.ApplyDamage`）。**待落地**：回收残骸 UI 入口、仓库容量加成数值（数值待裁定） |
| sf-23 | 侦察塔重构同步：`TeamVisionService` + `GameSessionBootstrap` 对齐户外设施版 | P3（最低） | 已关闭 → |
| sf-35 | 需求驱动指挥：`WorkPlan` 运行时对象与指派服务 | P1 | 部分符合 | 规则框架 sy-37 大部分已定；**运行时对象已落地**（2026-07-17，`Yanxu.WorkPlan` 程序集）：`WorkPlan`（未指派/已指派/已取消三态 + 四类二级参数 + 行动表占位 `OrderIndex`）、`WorkPlanRegistry`（全局清单 + 按队查询）、`WorkPlanAssignService`（候选枚举 + 直线距离+空闲优先排序 + 自动/手动指派 + 新建队伍回调）、`WorkPlanModifyService`（单条移除不清空指令表 + 第三层添加/替换）、`WorkPlanCancelService`（三级处置零惩罚 + 占位清理 + 工作中断）；编辑器自测 41 项全部通过；`GameSessionBootstrap` 已接入；**Team/Turn 运行时接线已落地**（2026-07-17：`ConnectWorkPlanRuntime` 注入真实委托——候选枚举取 `TeamUnitService` 存活队伍 + 能力标签映射 + 指令表待办计数、单条移除 / 添加替换经 `UnitCommandTableService`、计划→指令转换 `BuildCommandFromPlan`）；**阵亡回退已落地**（`HandleTeamDefeatedForWorkPlans` + `PrototypeContext.AdvancePhase` 扫描阵亡队伍退回未指派）；**待落地**：日志联动（sf-36）、工作清单 UI、参数编辑、行动表占位清理（行动表运行时未建）→ [平台与操作 · 需求驱动指挥](../02-系统设计/01-核心体验/平台与操作.md#需求驱动指挥工作计划) |
| sf-36 | 游戏日志运行时：`GameLogService`、消息等级与待办判定 | P2 | 待讨论 | 规则框架 sy-38（三种消息等级 info/warn/error、**黄红标记**、已知/待办双模式）；需新建**日志条目**运行时对象（等级、消息正文、待办标识、关联实体引用、**标记颜色**）、**日志列表存储**、**待办消息判定与自动消解**服务（按回合末或事件驱动触发重判）、**日志→实体联动**（点击跳转到工作计划清单等）；提醒到游戏日志的通路规则（哪些事件触发哪条消息）**待定** → [平台与操作 · 游戏日志](../02-系统设计/01-核心体验/平台与操作.md#游戏日志) |

### 交战、势力与外部交互

| ID | 问题 | 优先级 | 状态 | 备注 |
|----|------|--------|------|------|
| sf-24 | 交战结算：减员、结构损伤、设施耐久、反击 | P1 | 部分符合 | 三轨框架 sy-16；**战斗模块已建**（`Yanxu.Combat` 程序集，2026-07-17）：`AbilityParamAttackSO`（对齐表 6 `ability_param_attack`，`Configs/AttackParams` SO 管线优先 + 代码默认兜底）、`CombatDamageCalculator`（`damage = max(0, 攻击效果×人数比 − 防御能力×人数比)`，用户当轮裁定）、`CombatSettlementService`（造成伤害 → 逐 Applicable 结构减免→承伤 1:1 → 剩余转人口损失 → 反击判定）、`AttackExecutor`（`TryAttack`：目标/射程/敌我校验 + 即时结算）、`CounterattackExecutor`（一次交战至多一次反击、禁止连锁）、`DefaultCombatStructureResolver`（城墙 `populationLossReduction` + 城区完整度扣减、废墟阈值对齐 L4 表 20%）；`TeamCommandKind.Attack` 即时分支 + `Capability.Attack` Tag + 民兵/步兵/弓手模板已接；**已完成**：① 结构减免系数 SO 化（`FacilityDefsSO.Entry.damageReductionRatio` + `DistrictDefsSO.Entry.damageReductionRatio` + `DefaultCombatStructureResolver` 从 def SO 优先级读取 + `DesignTableImporter` 导入 L4/L5 新列 + CSV 数值已填（城墙 0.50 / 城区 0.20 / 生产仓储 0.15 / 轻型 0.10 / 陷阱 0.00））+ ② 损伤来源日志（`DamageSourceType` 枚举 + `FacilityDestructionService.ApplyDamage` / `DefaultCombatStructureResolver.ApplyStructureDamage` 记录日志）+ ③ 响应行为每回合上限（`CellResponseConfig.MaxActivationsPerTurn` + `CellResponseRegistry` 回合计数器+超限跳过，2026-07-19 全部编译零错误）；**待落地**：`ability_param_attack` 战力数值 SO 资产待策划填表（当前用代码占位值，D-23）、敌我判定接 `faction_relation_state.is_hostile`（当前按势力不同判敌对）、周内减员 vs 周总结顺序（D-27）、遭遇战 AI（保守/激进）未接、战报 UI |
| sf-25 | 领袖关系、组织传导、贸易与商队履约 | P1 | 部分符合 | 规则多已定；**关系存储 / 组织传导 / 人口损失折算已落地**（`Yanxu.Relation` 程序集，sy-29，2026-07-17）；委托面板 / 关系行动 / 商队 / 贸易价表 **待落地**（sy-27、sy-28） |
| sf-26 | 外部城市 AI 回合与粮食抽象 | P2 | 待讨论 | sy-33 |

### 环境系统与关系系统（新程序集框架）

| ID | 问题 | 优先级 | 状态 | 备注 |
|----|------|--------|------|------|
| sf-30 | `Yanxu.Environment` 程序集：光带引擎、环境效果、跨图层修正 | P1 | 待讨论 | 框架设计：[环境系统 · 程序设计](../03-程序设计/01-架构总览/环境系统.md)；依赖 sy-01、sy-07 |
| sf-31 | `Yanxu.Relation` 程序集：关系存储、组织传导、委托贸易、关系行动 | P1 | 部分符合 | 框架设计：[关系系统 · 程序设计](../03-程序设计/01-架构总览/关系系统.md)；依赖 sy-27～sy-32；**已落地切片**：Storage（LeaderRelationRecord / LeaderRelationProfile / RelationStore + RelationTier）、Organization（OrganizationGraph / OrganizationConduction + OrganizationConfigSO）、Loss（PopulationLossTracker · 双轨 2:1 / 4:1 + 当场结算 + 配置化）、Config（OrganizationConfigSO + RelationSystemConfigSO）、RelationOrchestrator + TurnPhase.RelationAction 枚举、**Delegation**（DelegationBoard · 恒 3 条补位 + 每周期一次刷新 + 招募 Gate，2026-07-17）、**Actions**（IRelationActionHandler 接口注入 + RelationActionService · 脱离/效忠判定，2026-07-17）、**City 侧处理器**（CityControlStateStore + CityRelationActionHandler · city_control_state 状态转换 + 守卫，City 自测 27 项通过，2026-07-17）；编辑器自测 79 项全部通过；**未落地**：Trade / Dialogue / LeaderRelationInitSO、委托生成工厂模板池（依赖 sy-28 待裁定）、脱离/效忠后续效果（城区强制脱离、队伍转敌对、外部城消解、资源划归——挂钩 `OnDetachEffects` / `OnFealtyEffects` 已留，待外部城市城区与队伍运行时对象接入） |
| sf-32 | `TurnPhase.RelationAction`：回合第五阶段（关系行动） | P1 | 部分符合 | 框架设计：[关系系统 · 程序设计](../03-程序设计/01-架构总览/关系系统.md#回合阶段枚举更新)；**枚举已落地**（2026-07-17，`TurnPhase.RelationAction = 4`）；**编排器已接入行动检测**（`RelationOrchestrator.RunRelationPhase` = 关系行动检测（自动脱离）→ SnapshotTiers，2026-07-17）；`TurnFlowController` 接入 **待落地**，且被一项**待裁定**阻塞：周总结子步骤位置——留在环境行动内，还是移到关系行动之后（须用户裁定后才能改 `TurnFlowController` 推进序列） |

### 配置、存档与工程化

| ID | 问题 | 优先级 | 状态 | 备注 |
|----|------|--------|------|------|
| sf-27 | 策划表 CSV → SO 导入与校验管线 | P1 | 部分符合 | L1～L5 表在仓；**Editor 校验管线已统一**（2026-07-17：`DesignTableImporter` 菜单校验 L0~L6 七表全通过；L1 已导入 SO）；**SO 类型已落地**（L2 `EnvironmentDefsSO` / L4 `DistrictDefsSO` / L5 `FacilityDefsSO` / L0 `CrossModifiersSO`）；**L0/L2/L4/L5 导入菜单已落地**（2026-07-19：扩展 `DesignTableImporter` 五个导入入口，经 MCP 操作 Plastic SCM 侧源码直接落地）；**待落地**：运行时启动校验
| sf-28 | 存档：回合态、拓扑、揭示层、资源与人口快照 | P2 | 待讨论 | Core 模块职责已列；**未实现** |
| sf-29 | 各模块与 [回合阶段](../02-系统设计/07-玩法循环/回合与行动表.md#回合阶段) tick 边界文档化 + 测试 | P2 | 待讨论 | [模块划分 · 待确认](../03-程序设计/01-架构总览/模块划分.md#待确认事项) |

### 城区链接与迁移城区

| ID | 问题 | 优先级 | 状态 | 备注 |
|----|------|--------|------|------|
| sf-39 | 链接即时命令入口：修建/拆除即时结算、编辑连接批量结算 | P1 | 部分符合 | sy-15 已闭合；规则见 [分离与拆解](../02-系统设计/03-图层与地点/建筑层/分离与拆解.md)、[连接与多核心](../02-系统设计/03-图层与地点/建筑层/连接与多核心.md)。**① 链接即时命令已落地**（2026-07-19：`LinkCommandService.TryBuildLink`/`TryDismantleLink`，详见 sf-10）；**待落地**：② 编辑连接批量结算（进入编辑态后高亮相邻格、点选切换标记新建/保留/拆除，确认时一次调用多条修建/拆除命令并统一结算）；③ 再连接自动匹配（仅一个相邻格时自动链接） |
| sf-40 | 迁移城区：地基占位、链接重组预约、中断处置 | P1 | 待讨论 | sy-15 已闭合。**需落地**：① 迁移城区工作（`WorkTypeConfigSO` 城中 `district_relocate`，对自己工作、互斥，对照 3 回合）；② 预占地基标记（目标格 `OccupiedByRelocatingDistrict` 占位 + 原格 `Relocating` 状态，两格均禁止其它用途）；③ 链接重组管线——规划时记录预约（新建数量 × 15 金属从库存预扣），迁移完成结算时 `DisconnectAll` 旧链接 + 按预约 `Connect` 新链接；④ 中断处置——取消或外部打断时释放预占、退还未消耗的预约金属、原位城区恢复。依赖 [sf-39](#sf-39) 链接命令管线 |
| sf-41 | 城区链接与迁移城区 UI | P2 | 待讨论 | sy-39 待补项。**需落地**：① 连接/分离/编辑连接界面（高亮相邻格、点选切换链接、确认预览新建/保留/拆除及合计金属消耗）；② 迁移城区规划界面（目标格选取 + 链接重组方案指定 + 资源预约预览） |

## 交叉引用

下列 **sy** 主归属在 [系统追踪](./待细化追踪-系统.md)；本轨上表已列对应 **sf** 实现项，此处只链规则正文。

| 主 ID | 程序落地（本轨） | 规则文档 |
|-------|------------------|----------|
| sy-01 | sf-15 | [地图与移动 · 太阳照射](../02-系统设计/02-地图与世界/地图与移动.md#太阳照射区与移动停用) |
| sy-07 | sf-14 | [地图图层 · 响应检测](../02-系统设计/03-图层与地点/地图图层.md#响应检测与行为触发) |
| sy-08 | sf-10、sf-17 | [连接与多核心](../02-系统设计/03-图层与地点/建筑层/连接与多核心.md) |
| sy-15 | sf-10、sf-39、sf-40 | [分离与拆解](../02-系统设计/03-图层与地点/建筑层/分离与拆解.md)、[连接与多核心](../02-系统设计/03-图层与地点/建筑层/连接与多核心.md) |
| sy-19 | sf-11、sf-11a～sf-11d、sf-09a、sf-09b | [地图与移动 · 航行态](../02-系统设计/02-地图与世界/地图与移动.md#航行态占格单位边界与禁用功能-sy-19) |
| sy-39 | sf-41 | [分离与拆解](../02-系统设计/03-图层与地点/建筑层/分离与拆解.md)（UI 待补项） |
| sy-23 | sf-05、sf-18 | [城市管理系统](../02-系统设计/04-资源与人口/城市管理系统.md) |
| sy-37 | sf-35 | [平台与操作 · 需求驱动指挥（工作计划）](../02-系统设计/01-核心体验/平台与操作.md#需求驱动指挥工作计划) |
| sy-38 | sf-36 | [平台与操作 · 游戏日志](../02-系统设计/01-核心体验/平台与操作.md#游戏日志) |

## 已关闭项

| ID | 问题 | 结论 | 关闭日期 |
|----|------|------|----------|
| sf-23 | 侦察塔重构同步 | `TeamVisionService` 重构：废止 `activeBoostTurnByFaction` / `MarkWatchtowerBoostActive` / `IsWatchtowerBoostActive`（主动增益机制）；`ResolveUnitVisionRange` 改为单参 `(unit)`，新增侦察塔设施被动加算（`unitVisionBonus` + 地形修正值）注入接口 `Func<HexCoord,bool>` + `Func<HexCoord,int>`；`CityVisionSource` 移除 `ReadyWatchtowerCount`（城市不再提供视野覆盖）；`Recompute` 移除城市视野循环（仅保留他人城市记录）。`GameSessionBootstrap`：`CreateVisionService` 移除 `resolveReadyWatchtowerCount` 参数，新增 `watchtowerFacilityChecker` / `watchtowerTerrainModifier`（默认 null）；`EnumerateCityVisionSources` 移除 watchtower 计数；`TryActivateWatchtowerVisionBoost` 整方法废止 | 2026-07-19 |
| sf-33 | 相机插值平滑参数调校 | 平滑机制与速率上限已落地（`StrategyCameraRig`）；`panSmoothTime` / `zoomSmoothTime` 当前占位默认值（0.12s / 0.15s）转为纯调参项——架构完成，手感调校后仅需改常量值 → 转 [策划表轨 tb-05](./待细化追踪-策划表.md) | 2026-07-16 |
| sf-38 | `WorkTypeConfigSO`：18 类工作类型默认资产 | `WorkTypeConfigSO` 字段补全（`blocks_movement` / `applies_to_actor_kind`）；`DefaultTeamAbilityBootstrap` 18 类工厂 + `WorkTypeRegistry.RegisterDefaults` 扩至 20 条；18 个 `.asset` 生成到 `Resources/Configs/WorkTypes/`；`WorkTypeConfigGenerator` Editor 菜单可随时再生；编译零错误 | 2026-07-19 |
| sf-14 | 地图图层响应检测器与行为触发顺序 | 检测架构已完成：`CellResponseConfig`（锚定格 + 检测半径 `CheckRange` + 优先级 `Priority` + 关注行动种类 + 触发行为）、`CellResponseRegistry`（范围匹配 + 优先级排序）、`GridResponseDetector`（通用数据驱动检测器，替代逐个手写检测器）、`FacilityResponseBootstrapper`（设施→响应配置注册）、`ResponseCheckPipeline`（Move 前后双检）；具体响应条目由策划逐条填表 → 转 [策划表轨 tb-06](./待细化追踪-策划表.md)；规则轨 sy-07 同日闭环 | 2026-07-16 |

## 修订记录

| 日期 | 版本 | 说明 |
|------|------|------|
| 2026-07-10 | 1.0.0 | 从全库单表拆出程序轨 |
| 2026-07-10 | 1.1.0 | 开放项改用 **sf-NN** 编号；**清空**已关闭项表（保留节结构） |
| 2026-07-10 | 1.2.0 | 废止 `待细化追踪.md` 索引；本文件为程序轨唯一入口 |
| 2026-07-11 | 2.0.1 | 如何维护新增「禁止臆测补全」行 |
| 2026-07-11 | 2.0.0 | **按模块补全 sf-08～sf-29**；说明与 [设计缺口清单](../03-程序设计/设计缺口清单.md) 分工；标注原型 vs 未落地现状 |
| 2026-07-15 | 2.1.0 | sf-17 断链修复：原 `01-草稿/归档/粮食与周总结/` 目录已移除，重链至 [粮食与周总结数据结构](../03-程序设计/03-数据字典/粮食与周总结数据结构.md)；补代码已存在注记 |
| 2026-07-16 | 2.2.0 | sf-02/sf-03/sf-04 城市能力求值服务落地（`CityCapabilityService` + `CityCapabilityFlagsSO`）；sf-08 回合中断与行动表扩展落地（`EncounterInterruptHandler` + `ActionTableExtensions`）；sf-09/sf-12 工作区开销落地（`WorkZoneOverheadService` + `AdvanceWithParticipants`）；sf-19 编组补员落地（`TeamFormationService` + `ReinforcementOrchestrator` + `HeadcountFactorCurve`）；sf-21 资源揭示落地（`RevealIntegrationBridge` + `ResourceRevealService`）；全部接入 `GameSessionBootstrap` |
| 2026-07-16 | 2.3.0 | 新增 sf-33：相机插值平滑时长参数待调校；平滑机制与平移速率上限已在 `StrategyCameraRig` 落地（上限当日由每秒 5 格调整为**每秒 10 格**，并改为随**屏幕缩小倍率**等比放大） |
| 2026-07-16 | 2.4.0 | 参数配置化整改：`DistrictDismantleRepairService` 硬编码平衡数值（拆解 −10%/+10 金属、修复 +10%/20 金属、废墟阈值 20%）迁入 `DistrictDismantleRepairConfigSO`，经 `GameSessionBootstrap` 加载（`Configs/DistrictDismantleRepair_Default`），符合 [SO 配置与能力通道规范](./SO配置与能力通道规范.md) |
| 2026-07-16 | 2.5.0 | sf-20 核对补记：`TryScoutScan` / `TryProspectScan` / `TryTransport` 无 executor；侦察进入揭示、装卸货指令、`TeamCargoLoadService` 均未接线 |
| 2026-07-16 | 2.6.0 | **视野系统落地（sy-36 完整方案）**：`VisionKnownLayerStore`（势力可见集 + 持久已知层 + 他人城市记入）、`WatchtowerConfigSO`（被动 +2 / 主动 +50%，`Configs/Watchtower_Default`）、`TeamVisionService`（单位视野 = ⌈满编 × 人数比⌉、城市视野 = 基础 + 侦察塔加算、覆盖重算、他人单位实时可见性查询、扫描能力进入视野即时揭示种类）；`GameSessionBootstrap.TryActivateWatchtowerVisionBoost` 扣 20 能源；`PrototypeContext` 移动 / 回合推进后重算；编辑器冒烟测试通过；sf-20 / sf-21 / sf-23 相应收窄 |
| 2026-07-16 | 2.6.0 | 新增 sf-34：地图显示模块初版落地（`Yanxu.MapView` 程序集、平顶朝向裁定、DTLM 色卡颜色显示），后续图层上屏与迷雾待讨论 |
| 2026-07-16 | 2.7.0 | 新增 sf-35：需求驱动指挥 `WorkPlan` 运行时对象与指派服务 |
| 2026-07-17 | 2.8.0 | **sf-35 收窄**：状态改为部分符合；新增指派修改服务、行动表占位、新建队伍三配置项；与 sy-37 已定项对齐 |
| 2026-07-17 | 2.9.0 | **sf-35 收窄**：新增取消工作计划服务、自动指派（直线距离+空闲优先）、资源预留时机（仅指派时预留）；与 sy-37 1.37.0 对齐 |
| 2026-07-17 | 2.10.0 | **sf-35 收窄**：新增阵亡回退服务、日志联动服务；**sf-36** 更新：补黄红标记、日志→实体联动；与 sy-37 1.38.0、sy-38 对齐 |
| 2026-07-17 | 2.11.0 | **废止黄色指针**：sf-35 移除指针服务，替为日志联动；sf-36 补标记颜色字段 |
| 2026-07-17 | 2.12.0 | **需求驱动指挥 · 运行时对象落地**：`Yanxu.WorkPlan` 程序集创建（WorkPlan + Registry + AssignService + ModifyService + CancelService）；编辑器自测 41 项全通过；GameSessionBootstrap 接线（占位委托，待 Team/Turn 模块运行时接入） |
| 2026-07-16 | 2.8.0 | **sf-33 闭环**：相机平滑参数架构已完成（`StrategyCameraRig`），`panSmoothTime` / `zoomSmoothTime` 仅为调参项；从开放项移入已关闭项，占位值转 [策划表轨 tb-05](./待细化追踪-策划表.md) |
| 2026-07-16 | 2.9.0 | **sf-14 闭环**：响应检测改为纯数据驱动——`CellResponseConfig` 以「检测半径 + 优先级 + 关注行动」建模（废止 `ResponseCheckMode` 三态枚举）、`CellResponseRegistry` 范围匹配并按优先级取首个命中、`GridResponseDetector` 为通用检测器；具体响应条目转 [策划表轨 tb-06](./待细化追踪-策划表.md) |
| 2026-07-17 | 2.10.0 | **关系系统 · sy-29 实现落地**：`Yanxu.Relation` 程序集创建（Storage / Organization / Loss / Config 四个切片）；编辑器自测 38 项全部通过；`GameSessionBootstrap.Relations / Organizations / RelationConduction / PopulationLoss` 接线完成；sf-25 / sf-31 状态更新为部分符合 |
| 2026-07-17 | 2.11.0 | **关系系统 · 配置化与编排器落地**：`RelationSystemConfigSO`（全局配置 SO，含 `CreateRuntimeDefault()`）；`TurnPhase.RelationAction` 枚举 = 4；`RelationOrchestrator` 骨架（当前链入 SnapshotTiers）；PopLossTracker / OrgConduction 接受可选 `BindConfig()`；编辑器自测扩至 48 项全通过；sf-31 / sf-32 状态更新 |
| 2026-07-17 | 2.13.0 | **新增 sf-37 · 初始城市结构配置化落地**：`InitialCityLayoutConfigSO` + `InitialCityAssembler` + 默认布局资产；`GameSessionBootstrap` / `PrototypeContext` 接线（布局缺省回退旧星形注册）；`DistrictInstanceState` 补 `DistrictTypeId`；`DistrictConnectionGraph` 补带类型 / 完整度的注册重载与 `DisconnectAll`；编辑器冒烟测试通过 |
| 2026-07-17 | 2.13.1 | **城市仓库堆叠上限修复**：`ItemInventory.TryAdd` 新增可选参数 `ignoreStackLimit`（默认 `false` 保持队伍背包行为不变）；城市仓库路径（仓库播种、拆解产出、设施生产产出）传入 `true` 绕过 per-item 堆叠上限（原粮 50 / 金属 99 上限导致配置量被静默截断）；`EnsureWarehouseStock` 恢复直写逻辑；编辑器端到端验证 `food=200 metal=100 pop=120` 与配置一致 |
| 2026-07-17 | 2.14.0 | **sf-22 设施落地（第二波）**：温室 ResourceConvert 全链路（枚举新增 ResourceConvert + ConfigSO.inputConversionPerWork + ProductionService 扣输入能源产输出食物 + Bootstrap 注册 能源 50→食物 150）；建造消耗真实扣减（LifecycleService 接入 resolveCityInventory → TryPayBuildCost 遍历 resourceCosts 扣城市库存）；桥梁几何守卫（方向 0-5 贯穿 site→instance 全链 + 对向两端非阻断地形校验 + HexCoord.GetNeighborAt + FacilityBuildRuleEvaluator.ValidateBridgeGeometry） |
| 2026-07-17 | 2.15.0 | **关系系统 · 委托面板与关系行动落地**：Delegation（LeaderDelegation / DelegationBoard · 恒 3 条补位 + 每周期一次刷新（已定）+ 招募 Gate + 完成奖励与失败扣 1/4）；Actions（IRelationActionHandler **接口注入已定** + RelationActionService · 招募未效忠 + R ≤ −50 自动脱离、已招募 + R ≥ 100 可效忠）；RelationOrchestrator 接入行动检测；编辑器自测扩至 79 项全通过；sf-31 / sf-32 状态更新 |
| 2026-07-17 | 2.16.0 | **City 侧关系行动处理器落地**：`CityControlStateStore`（cityId → city_control_state 运行时存储 + 变化通知）+ `CityRelationActionHandler`（实现 IRelationActionHandler：招募状态查询 + 脱离/效忠守卫与状态转换 + 后续效果挂钩 `OnDetachEffects` / `OnFealtyEffects` 占位）；`Yanxu.City` 增引 `Yanxu.Relation`；Bootstrap 注入完成；City 编辑器自测 27 项全通过；sf-31 收窄、sf-32 补记待裁定（周总结子步骤位置） |
| 2026-07-17 | 2.16.0 | **sf-22 设施落地（第三波 · 航道效果接线）**：`SailingGridDamageService`（航行每步对占用格所属城区扣结构完整度，基准取 `MobileCityConfigSO.loadCostPerStep`，格上有有效航道则损伤减半；`GameSessionBootstrap.SailingGridDamage` 穿线、`PrototypeContext.TryExecuteCityMoveStep` 接入）；`FacilityPathCostProvider`（地形消耗对齐 `L1_terrain_defs.csv`：平原 1 / 丘陵 2 / 山地 5 / 河流 4 / 裂谷 1 / 沼泽 3，通道与航道格步长→1）；`HexPathService.FindPath` 新增可选 `getCellCost` 回调（缺省行为不变）；`TeamUnitService` 移动结算改为按格消耗累计步数预算（至少推进 1 格）；`PrototypeContext` 单位寻路 / 可达范围 / 剩余回合预估接入成本回调（城市寻路仍按每格 1）；城市航行移动时同步平移 `DistrictConnectionGraph` 未分离城区的占格坐标，修复移动后 footprint 与连接图脱节 |
| 2026-07-17 | 2.17.0 | **sf-22 设施落地（第四波 · 失效与残骸）**：`FacilityDestructionService`（`ApplyDamage` 耐久扣减 → ≤20% 阈值转残骸；`TryDestroy` 直接击毁；`TryRecycleRuin` 回收原建造金属 10% 向下取整并释放槽位）；`FacilityInstanceState.IsRuin` + `FacilityInstanceStore.RemoveInstance`；残骸格同类型重建 80% 资源（`TryPayBuildCost` 折扣 + 完成建造时移除旧残骸 + 共存校验放行同类型残骸、不同类型残骸仍占位阻断）；地形类失效后果经 `OnFacilityDestroyed` 事件在 `PrototypeContext` 接线（格内单位阵亡清占格、格内非核心城区 DisconnectAll + 分离正常扣减 + 额外 40% 结构损伤、footprint 同步）；`recycle_facility` 工作类型注册 + `WorkCompletionDispatcher` 完成分发；**待接**：回收指令入口（UI/指令枚举）、交战损伤调用 `ApplyDamage`（sf-24） |
| 2026-07-17 | 2.18.0 | **sf-37 校验收紧（三项已定规则落地）**：`InitialCityAssembler.Validate` 新增——① `link_not_adjacent` 链两端占格须六边形相邻（对齐连接与多核心 · 拓扑示例：直线 A—B—C 时 A 与 C 不直连）；② `ruins_integrity_mismatch` / `integrity_below_ruin_threshold` 废墟状态与结构完整度须一致（低于阈值即废墟，阈值经 `RegisterCityFromLayout` 取 `DistrictDismantleRepairConfigSO.ruinThreshold`，缺省 0.2）；③ `cell_off_map` 城区占格须落在地图格内（`PrototypeContext` 传入 `GridStore.Exists`）；SO Tooltip 同步补充约束说明；顺带修复 `TeamActionExecutionService` 攻击分支 `attackFail` 未赋值编译错误；编辑器编译 + 播放冒烟均通过 |
| 2026-07-17 | 2.19.0 | **sf-24 战斗模块落地（首版）**：`Yanxu.Combat` 程序集创建——`AbilityParamAttackSO`（对齐表 6，`param_profile_id` 主键 + `Configs/AttackParams` Resources 管线，SO 资产优先、代码默认兜底）、`CombatGlobalConfigSO`（最小伤害 / 结构减免默认系数 / 城区废墟阈值 20% 对齐 L4 表）、`CombatDamageCalculator`（**已定**：`damage = max(0, base_attack_power×人数比 − base_defense_power×人数比)`，向下取整）、`CombatSettlementService`（受到伤害两段式：逐 Applicable 结构 减免→承伤 1:1 → 剩余转人口损失）、`CombatRangeService`、`AttackParamRegistry`、`CombatSessionServices`；Team 侧：`AttackExecutor`（TryAttack 即时结算：目标解析 + 射程 + 敌我校验）、`CounterattackExecutor`（一次交战至多一次、禁止连锁、反击方需 `allow_counterattack` 且原攻击方在反击范围内）、`TeamCommandKind.Attack` 专用分支、`Capability.Attack` Tag、民兵 / 步兵 / 弓手模板 GA/GE、`TeamUnitConfigSO.attackParamId` → `TeamInstanceState.AttackParamId`；UI 侧：`DefaultCombatStructureResolver`（城墙 `populationLossReduction` 计入减免 + 设施承伤合流 `FacilityDestructionService.ApplyDamage`、城区完整度扣减）；Bootstrap `WireCombat` + `PrototypeContext` executor 注册；编译零错误 |
| 2026-07-17 | 2.20.0 | **批量落地（直接可解决项）**：① sf-02/sf-06 gate 测试矩阵（`CityCapabilityServiceTests` T-01~T-29 全通过）；② sf-20 运输链补全（`LoadCargo`/`UnloadCargo` 指令 + `LoadCargoExecutor` + `unload_cargo` 工作类型 + 完成分发接线）；③ sf-22 回收残骸指令入口（`RecycleFacility` 指令 + `RecycleFacilityExecutor`）；④ sf-09 拆解/修复城区工作类型注册与映射补全；⑤ sf-15 核对确认 Sun 模块已落地并收窄；⑥ sf-35 工作计划 Team/Turn 接线（`ConnectWorkPlanRuntime` 真实委托 + `BuildCommandFromPlan`）与阵亡回退（`HandleTeamDefeatedForWorkPlans` + `AdvancePhase` 扫描）；⑦ sf-13/sf-27 导入与校验管线首版（`DesignTableCsv` + `DesignTableImporter` 校验 L0~L6 全通过 + L1 → `TerrainDefsSO` 导入 + `FacilityPathCostProvider.OverrideCosts` 接线）；全程编译零错误 |
| 2026-07-19 | 2.22.0 | **侦察塔统一为单一野外设施版本**：`WatchtowerConfigSO` 重构（`cityVisionBonusHex` / `unitVisionBonusPct` / `cityBaseVisionRange` → `unitVisionBonus`（int，+3 加算），无主动）；从 `L4_district_defs.csv` 中移除（`status=removed`）→ `L5_facility_defs.csv` 为唯一归属（建造 30 金属 / 3 回合，所在格单位视野 +3）。Capability Tag 从 `District.Watchtower` / `District.Watchtower.Ready` / `Buff.Watchtower.UnitVisionBoost` → `Capability.Facility.Watchtower`。`DistrictModuleCostConfigSO` 删除 watchtower 条目。`TeamVisionService` 与 `GameSessionBootstrap` 待同步（依赖项：`TryActivateWatchtowerVisionBoost` 废止，`ResolveCityVisionRange` 移除侦察塔加算，`EnumerateCityVisionSources` 移除 `resolveReadyWatchtowerCount`） |
| 2026-07-19 | 2.23.0 | **修复规则裁定 · 程序影响**：修复工作改为每回合独立结算——不再积满后一次性触发，每回合按工作量比例结算恢复量与金属消耗；需在完成分发中添加终止条件（达 100% 自动终止、金属不足该轮终止）。sf-09 同步 |
| 2026-07-19 | 2.24.0 | **sf-23 闭环**：`TeamVisionService` 重构——废止主动增益（`activeBoostTurnByFaction` / `MarkWatchtowerBoostActive` / `IsWatchtowerBoostActive`）；`ResolveUnitVisionRange` 改为单参 `(unit)`，新增侦察塔设施被动加算 + 地形修正注入；`CityVisionSource` 移除 `ReadyWatchtowerCount`；`Recompute` 移除城市视野循环。`GameSessionBootstrap`：`CreateVisionService` 移除 `resolveReadyWatchtowerCount`，新增 `watchtowerFacilityChecker` / `watchtowerTerrainModifier`；`TryActivateWatchtowerVisionBoost` 废止 |
| 2026-07-19 | 2.31.0 | **阻塞项登记**：新增「阻塞项：实现侧等待规则裁定」节——sf-block-01（委托模板池工厂，阻塞于 sy-28）、sf-block-02～04（外部城市 AI 经济，阻塞于 sy-33）；sy-28/sy-33 系统轨条目同步标注阻塞路径 |
| 2026-07-19 | 2.25.0 | **sf-10 落地（链运行时图结构）**：`LinkConfigSO` 创建（修建 15 / 回收 1 / 重建 12 金属 + `CreateDefault()` 兜底）；`DistrictConnectionGraph` 新增 `removedEdges` 历史追踪 + `HasEdge` / `HasRemovedEdge` / `EdgeCount` 查询；`Connect` / `Disconnect` / `DisconnectAll` 同步维护 `removedEdges`（用于原址重建 80% 折扣判定）；链接修建/拆除即时命令入口待接（城市金属库存 + 相邻校验） |
| 2026-07-19 | 2.26.0 | **sf-15 落地（太阳下游接线）**：`LightBandService` 创建（统一光带查询 + `IsInDarkZone` / `IsInTwilight` / `IsInDaylight` / `IsGlobalDark` 便捷方法 + `OnSunMotionDisabled` 事件转发）；`SunMotionService` 新增 `OnSunMotionDisabled` 事件（n≤0 停用时触发）；下游 debuff / 移动停用具体效果待 sy-01 裁定后接入 |
| 2026-07-19 | 2.27.0 | **sf-21 落地（迷雾渲染）**：`FogStateProvider` 创建（三态查询：Unexplored/Known/Visible + 便捷判断方法，基于 `VisionKnownLayerStore`）；`FogRenderer` 创建（六边形遮罩 mesh 实例化 + 三态材质切换 + `MarkDirty` 增量刷新 + `LateUpdate` 轮询）；迷雾材质资产（ShaderGraph/Shader）与 Bootstrap 接线待后续完成 |
| 2026-07-19 | 2.28.0 | **sf-13/sf-27 落地（SO 类型）**：`EnvironmentDefsSO`（L2_environment_defs）/ `DistrictDefsSO`（L4_district_defs）/ `FacilityDefsSO`（L5_facility_defs）/ `CrossModifiersSO`（L0_cross_modifiers）创建；L2~L5 表导入到对应 SO 的 `DesignTableImporter` 菜单扩展需 Plastic SCM 侧 `DesignTableImporter.cs` / `DesignTableCsv.cs` 源码
| 2026-07-19 | 2.30.0 | **sf-13/sf-27 导入菜单闭环**：`DesignTableImporter` 经 MCP 扩展——新增「导入 L0 跨层修正表」「导入 L2 环境定义表」「导入 L4 城区定义表」「导入 L5 设施定义表」四个菜单项 + 通用 `ReadTable` / `SaveAsset` 辅助方法；`Yanxu.Map.Editor.asmdef` 补引 `Yanxu.City` / `Yanxu.Environment` / `Yanxu.Facility`；修复 `MapImporterWindow` 命名空间冲突（`System.Environment` → 完整限定符）；`GameSessionBootstrap.LinkConfig` 接线（`LinkConfigSO` 加载 + 属性）；编译零错误 |
| 2026-07-19 | 2.29.0 | **sy-13 闭合分派**：新增 sf-38——`WorkTypeConfigSO` 18 类 SO 类型与默认资产（sy-13 系统设计闭合，SO 创建转程序轨）；sf-09 链入 sf-38 |
| 2026-07-19 | 2.30.0 | **sf-38 落地**：`WorkTypeConfigSO` 新增 `blocks_movement` / `applies_to_actor_kind`；`WorkSubjectKind` 新增 `Team`；`DefaultTeamAbilityBootstrap` 18 类全覆盖工厂（移除 prospecting/prospect_scan/recycle_facility 废弃工厂）；`WorkTypeRegistry.RegisterDefaults` 扩至 20 条（18 类 + scout_scan + dock/sail 由 MobileCityConfig 覆盖）；`MobileCityConfigSO` 离港/入港默认 2 回合 + `applyHeadcountFactor=false`；清理 `ProspectingMilestone` 引用与 `TryApplyMidWorkMilestone`；编译零错误 |
| 2026-07-19 | 2.24.0 | **sf-23 闭环**：`TeamVisionService` 重构——废止主动增益（`activeBoostTurnByFaction` / `MarkWatchtowerBoostActive` / `IsWatchtowerBoostActive`）；`ResolveUnitVisionRange` 改为单参 `(unit)`，新增侦察塔设施被动加算 + 地形修正注入；`CityVisionSource` 移除 `ReadyWatchtowerCount`；`Recompute` 移除城市视野循环。`GameSessionBootstrap`：`CreateVisionService` 移除 `resolveReadyWatchtowerCount`，新增 `watchtowerFacilityChecker` / `watchtowerTerrainModifier`；`TryActivateWatchtowerVisionBoost` 废止 |
| 2026-07-19 | 2.31.0 | **阻塞项登记**：新增「阻塞项：实现侧等待规则裁定」节——sf-block-01（委托模板池工厂，阻塞于 sy-28）、sf-block-02～04（外部城市 AI 经济，阻塞于 sy-33）；sy-28/sy-33 系统轨条目同步标注阻塞路径 |
| 2026-07-19 | 2.31.0 | **sy-16 实现分派**：sf-24 新增三项待落地——结构减免系数 SO 化（`FacilityDefSO.damage_reduction_ratio`）、损伤来源日志（`combat_event_log` 扩字段）、响应行为每回合次数上限（设施响应计数器） |
| 2026-07-19 | 2.32.0 | **sy-19 实现分派**：sf-11 展开为 sf-11a～sf-11d 四子项（移除旧登录守卫 / 移除旧阻挡逻辑 / 搭载随城移动 / 倾轧+AI规避）；sf-09 新增 sf-09a（`interrupt_behavior` 改为 -1/0/n 数值字段）+ sf-09b（`is_precision_work` 被攻击/航行移动触发中断接线）；交叉引用 sy-19 链向新子项 |
| 2026-07-19 | 2.33.0 | **sy-15 闭合分派**：sf-09 移除旧「连接/分离待落地」引用（链接为即时操作不经过工作管线）；sf-10 扩编——新增编辑连接模式与迁移链接重组待落地，术语「链」→「链接」；sf-37 移除旧 sy-15 引用；新建 sf-39（链接即时命令/编辑连接批量结算）、sf-40（迁移城区地基占位/链接重组预约/中断处置）、sf-41（链接与迁移城区 UI）；交叉引用补 sy-15、sy-39 行 |
| 2026-07-19 | 2.34.0 | **sf-24 / sf-10 / sf-39 四项落地**：sf-24-① 结构减免系数 SO 化（`FacilityDefsSO.damageReductionRatio` + `DistrictDefsSO.damageReductionRatio` + `DefaultCombatStructureResolver` def SO 优先级读取 + L4/L5 CSV 数值填列 + `DesignTableImporter` 导入新列）；sf-24-② 损伤来源日志（`DamageSourceType` 枚举 + `FacilityDestructionService.ApplyDamage` / `DefaultCombatStructureResolver.ApplyStructureDamage` 日志记录）；sf-24-③ 响应行为每回合上限（`CellResponseConfig.MaxActivationsPerTurn` + `CellResponseRegistry` 回合计数器+超限跳过）；sf-10 / sf-39 链接即时命令入口（`LinkCommandService.TryBuildLink`（邻接校验+金属扣除+重建折扣）/ `TryDismantleLink`（连通性保护+金属回收）+ `GameSessionBootstrap.LinkCommands` 接线）；全部编译零错误 |
